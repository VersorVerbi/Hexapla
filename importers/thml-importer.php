<?php

namespace Hexapla;

function import_thml($simpleXml) {
  /*$metadata = $simpleXml->{'ThML.head'};
  $body = $simpleXml->{'ThML.body'};
  */
  // we might not need to do this because it looks like the ThML focus is on theological documents rather than Scripture itself
  // or, I think, it can at least wait until we're ready to start pulling in commentaries or somesuch
}
